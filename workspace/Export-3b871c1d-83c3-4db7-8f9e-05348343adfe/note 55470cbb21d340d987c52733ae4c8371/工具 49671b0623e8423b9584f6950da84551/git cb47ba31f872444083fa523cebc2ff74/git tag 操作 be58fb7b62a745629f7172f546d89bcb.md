# git tag 操作

```bash
git tag 0.14.7
git tag 0.14.7 -f

git push origin 0.14.7

git push --tags

# 输出最新的tag
git describe --tags  --abbrev=0
git for-each-ref --sort=taggerdate --format '%(refname:short) %(taggerdate:short)' refs/tags
```